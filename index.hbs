{{!< default}}

<!--
<div id="latest-post" class="container">
    {{#foreach posts}}
        {{#if @first}}
            {{> post-content}}
        {{/if}}
    {{/foreach}}
</div>
-->

<div id="post-index" class="container">  
    {{#foreach posts}}
        {{> post-content}}
        <img src="assets/img/twitter.jpg" height="32" width="32">
    {{/foreach}}
    <!--
    <ol class="post-list">
        {{#foreach posts}}
            <li class="post-stub {{post_class}}" >
                <a class="js-ajax-link"  title="{{title}} | {{@blog.title}}" href="{{url}}">
                    <h4 class="post-stub-title">{{title}}</h4>

                    <time class="post-stub-date" datetime="{{published_at}}">{{date format="DD MMMM YYYY"}}</time>

                    {{#if featured}}
                        <span class="post-stub-tag">Featured</span>
                    {{/if}}
                </a>
            </li>
        {{/foreach}}
    </ol>
    -->
    <div class="post-navigation">
        {{pagination}}
    </div>
</div>